/*13、计算函数的值
 * f(x)=3x+10 100>x>=0
 * f(x)=x^3-20 x>=100*/
int Function(int x){
    if(x>=0 && x<100) return (3*x+10);
    if(x>=100) return (x*x*x-20);
}
int main(){
    cout<<"请输入一个数字:"<<endl;
    int x;
    cin>>x;
    cout<<Function(x);
    return 0;
}

