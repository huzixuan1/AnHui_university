/*17、使用函数编写如下函数：
 * f(x)=1 x>0
 * f(x)=0 x=0
 * f(x)=-1 x<0*/
int Funct_one(int para){
    if(para>0) return 1;
    if(para==0) return 0;
    if(para<0) return -1;
    return 0;
}
int main(){
    float num;
    cout<<"请输入一个数字："<<endl;
    cin>>num;
    cout<<Funct_one(num);
    return 0;
}
